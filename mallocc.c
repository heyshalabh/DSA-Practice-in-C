#include<stdio.h>

    void main(){
        int n, i, *ptr, sum =0;

        pritnf("Enter No. Of  Elements : ");
        scanf("%d", &n);

        ptr = (int *)malloc (n*sizeof(int));

        for(i=0; i<n; i++){
            
        }
    }